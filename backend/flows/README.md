# Live Maestro flows

AppLab v0.2 always exposes the built-in `generic-smoke` flow.

Additional `.yaml` or `.yml` files placed in this directory are listed by
the Live Control Center and can be executed through the controller API.
Keep flows trusted: the backend deliberately accepts only files from this
allow-listed directory.
