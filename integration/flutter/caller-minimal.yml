name: AppLab Project Gate

on:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  applab:
    uses: riccardopinato/AppLab/.github/workflows/verify-flutter.yml@main
